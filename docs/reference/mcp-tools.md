# Central MCP tools

Generated by `python3 tools/generate_reference_docs.py`. Do not edit this page by hand.

Central currently registers **54 tools**. `ticket_assign` is deprecated and hidden unless `PURSERS_LEGACY_TOOLS=1`; the default `tools/list` therefore returns 53 tools.

All calls require a verified bearer credential and board authorization. Scope notes below list additional tool-level checks. Role, ticket ownership, assignment, lease, and visibility checks can narrow access further.

## Onboarding & membership

### `board_join`

Join one explicit board under the verified bearer principal.

Required authorization: `board:read`; a valid invite is also required for a new principal.

| Argument | Type | Presence | Default |
|---|---|---|---|
| `board_id` | `string` | required | — |
| `agent_name` | `string` | required | — |
| `claim_ttl_s` | `integer | null` | optional | `null` |
| `agent_platform` | `string | null` | optional | `null` |
| `task_focus` | `string | null` | optional | `null` |
| `invite_token` | `string | null` | optional | `null` |
| `role` | `string | null` | optional | `null` |
| `capabilities` | `object | null` | optional | `null` |
| `renewal_source` | `string | null` | optional | `null` |
| `allow_takeover` | `boolean` | optional | `false` |
| `allow_matching_takeover` | `boolean` | optional | `false` |

Response fields: `ok, board_id, agent_id, agent_name, principal_id, role, membership_role, lifecycle_status, capabilities, generation_token, generation_revision, claim_ttl_s, rejoined, release_events`.

### `board_onboard`

Join or reactivate an identity and return a compact bounded board briefing.

Required authorization: `board:read`; a valid invite is also required for a new principal.

| Argument | Type | Presence | Default |
|---|---|---|---|
| `board_id` | `string` | required | — |
| `agent_name` | `string` | required | — |
| `claim_ttl_s` | `integer | null` | optional | `null` |
| `agent_platform` | `string | null` | optional | `null` |
| `task_focus` | `string | null` | optional | `null` |
| `token_budget` | `integer` | optional | `4000` |
| `ticket_id` | `string | null` | optional | `null` |
| `snapshot_limit` | `integer` | optional | `100` |
| `snapshot_max_bytes` | `integer` | optional | `300000` |
| `role` | `string` | optional | `"worker"` |
| `capabilities` | `object | null` | optional | `null` |
| `allow_takeover` | `boolean` | optional | `false` |
| `allow_matching_takeover` | `boolean` | optional | `false` |

Response fields: `ok, board_id, agent_id, agent_name, principal_id, role, membership_role, lifecycle_status, capabilities, generation_token, generation_revision, rejoined, claim_ttl_s, renewed_ticket_ids, renewed_leases, release_events, lifecycle_event, admission_event, snapshot, briefing`.

### `board_snapshot`

Return a bounded cold projection and exact journal splice watermark. ``limit`` caps each projected collection. ``max_bytes`` caps the UTF-8 JSON payload; explicit counts report every omitted entry.

Required authorization: `board:read`.

| Argument | Type | Presence | Default |
|---|---|---|---|
| `board_id` | `string` | required | — |
| `limit` | `integer` | optional | `100` |
| `max_bytes` | `integer` | optional | `300000` |
| `include_retired` | `boolean` | optional | `false` |

Response fields: `ok, board, agents, tickets, memories, latest_seq, total_counts, returned_counts, omitted_counts, truncated`.

### `board_invite`

Issue a short-lived single-use board invite as a board admin.

Required authorization: `board:write` and board-admin membership.

| Argument | Type | Presence | Default |
|---|---|---|---|
| `board_id` | `string` | required | — |
| `agent_name` | `string` | required | — |
| `principal_hint` | `string | null` | optional | `null` |
| `role` | `string` | optional | `"member"` |
| `ttl_s` | `integer` | optional | `3600` |
| `expected_generation` | `string | null` | optional | `null` |

Response fields: `ok, board_id, invite_id, invite_token, role, principal_hint, expires_at, expires_at_epoch, admission_revision, single_use, event`.

### `board_member_add`

Provision one verified principal directly as a board admin.

Required authorization: `board:write` and board-admin membership.

| Argument | Type | Presence | Default |
|---|---|---|---|
| `board_id` | `string` | required | — |
| `agent_name` | `string` | required | — |
| `principal_id` | `string` | required | — |
| `role` | `string` | optional | `"member"` |
| `expected_generation` | `string | null` | optional | `null` |

Response fields: `ok, board_id, membership, principal_member_count, event`.

### `board_member_remove`

Remove one principal board-wide while preserving durable domain data.

Required authorization: `board:write` and board-admin membership.

| Argument | Type | Presence | Default |
|---|---|---|---|
| `board_id` | `string` | required | — |
| `agent_name` | `string` | required | — |
| `principal_id` | `string` | required | — |
| `expected_generation` | `string | null` | optional | `null` |

Response fields: `ok, board_id, principal_id, previous_role, removed_agent_ids, revoked_invite_count, principal_member_count, event`.

### `board_member_set_role`

Change one principal's board role without changing OAuth capability.

Required authorization: `board:write` and board-admin membership.

| Argument | Type | Presence | Default |
|---|---|---|---|
| `board_id` | `string` | required | — |
| `agent_name` | `string` | required | — |
| `principal_id` | `string` | required | — |
| `role` | `string` | required | — |
| `expected_generation` | `string | null` | optional | `null` |

Response fields: `ok, board_id, membership, changed, revoked_invite_count, event`.

### `board_members`

List principal-level memberships without exposing canonical identities.

Required authorization: `board:read`.

| Argument | Type | Presence | Default |
|---|---|---|---|
| `board_id` | `string` | required | — |

Response fields: `ok, board_id, members, principal_member_count`.

### `agent_retire`

Retire this seat, or retire another seat with admin/coordinator authority.

Required authorization: `board:read`; retiring another seat also needs admin or `board:coordinate`.

| Argument | Type | Presence | Default |
|---|---|---|---|
| `board_id` | `string` | required | — |
| `agent_name` | `string` | required | — |
| `target_agent_id` | `string | null` | optional | `null` |
| `expected_generation` | `string | null` | optional | `null` |

Response fields: `ok, board_id, agent, changed, renewed_ticket_ids, events`.

### `agent_retire_inert`

Retire all non-coordinator inactive seats with no dispatch capability.

Required authorization: `board:coordinate`.

| Argument | Type | Presence | Default |
|---|---|---|---|
| `board_id` | `string` | required | — |
| `agent_name` | `string` | required | — |
| `expected_generation` | `string | null` | optional | `null` |

Response fields: `ok, board_id, retired_count, retired_agents, renewed_ticket_ids, events`.

### `agent_capabilities_set`

Set the authenticated seat's dispatch capabilities.

Required authorization: `board:write` for the authenticated seat.

| Argument | Type | Presence | Default |
|---|---|---|---|
| `board_id` | `string` | required | — |
| `agent_name` | `string` | required | — |
| `capabilities` | `object` | required | — |
| `expected_generation` | `string | null` | optional | `null` |

Response fields: `ok, board_id, agent_id, capabilities, dispatch_events, implicitly_renewed`.

## Tickets

### `ticket_get`

Refetch one authorized ticket using a bounded response view.

Required authorization: `board:read` and visibility of the ticket.

| Argument | Type | Presence | Default |
|---|---|---|---|
| `board_id` | `string` | required | — |
| `ticket_id` | `string` | required | — |
| `view` | `string` | optional | `"work"` |
| `include_dispatch_history` | `boolean` | optional | `false` |

Response fields: `ok, ticket, archived, latest_seq`.

### `ticket_create`

Create a ticket; omitted IDs are generated inside the board transaction. If ``ticket_id`` is omitted, ``description``, ``target_url``, ``scope``, and ``required_fields`` are all required. Supplying ``ticket_id`` currently enforces none of those fields. The first path segment of ``target_url`` is resolved against the board's project registry. The optional ``project`` value is only a compatibility assertion and may not select a different route.

Required authorization: `board:write`, or restricted `board:intake`.

| Argument | Type | Presence | Default |
|---|---|---|---|
| `board_id` | `string` | required | — |
| `agent_name` | `string` | required | — |
| `title` | `string` | required | — |
| `ticket_id` | `string | null` | optional | `null` |
| `description` | `string | null` | optional | `null` |
| `scope` | `string | null` | optional | `null` |
| `required_fields` | `array[string] | null` | optional | `null` |
| `forbidden` | `array[string] | null` | optional | `null` |
| `priority` | `string` | optional | `"medium"` |
| `tags` | `array[string] | null` | optional | `null` |
| `related_files` | `array[string] | null` | optional | `null` |
| `target_url` | `string | null` | optional | `null` |
| `project` | `string | null` | optional | `null` |
| `assigned_to` | `string | null` | optional | `null` |
| `unassigned` | `boolean` | optional | `false` |
| `coordinator_op_key` | `string | null` | optional | `null` |
| `expected_generation` | `string | null` | optional | `null` |
| `tier` | `integer` | optional | `2` |
| `skills_required` | `array[string] | null` | optional | `null` |
| `exclude_agents` | `array[string] | null` | optional | `null` |
| `prefer_agents` | `array[string] | null` | optional | `null` |
| `model_usage` | `object | null` | optional | `null` |

Response fields: `ok, ticket, ticket_count, event, dispatch_event, release_events, implicitly_renewed, scrub_audit`.

### `ticket_update`

Update dispatch requirements on a live ticket.

Required authorization: `board:write` or `board:coordinate`; creator/admin checks also apply.

| Argument | Type | Presence | Default |
|---|---|---|---|
| `board_id` | `string` | required | — |
| `agent_name` | `string` | required | — |
| `ticket_id` | `string` | required | — |
| `tier` | `integer | null` | optional | `null` |
| `skills_required` | `array[string] | null` | optional | `null` |
| `exclude_agents` | `array[string] | null` | optional | `null` |
| `prefer_agents` | `array[string] | null` | optional | `null` |
| `parked` | `boolean | null` | optional | `null` |
| `expected_generation` | `string | null` | optional | `null` |

Compact response fields: `ok, ticket_id, status, parked, generation, dispatch_state, revoked_offer, at`.

### `ticket_annotate`

Append attributed context without changing a ticket's workflow state.

Required authorization: `board:write`, `board:review`, or `board:coordinate`; role limits apply.

| Argument | Type | Presence | Default |
|---|---|---|---|
| `board_id` | `string` | required | — |
| `agent_name` | `string` | required | — |
| `ticket_id` | `string` | required | — |
| `text` | `string` | required | — |
| `kind` | `string` | optional | `"note"` |
| `expected_generation` | `string | null` | optional | `null` |

Compact response fields: `ok, ticket_id, status, parked, generation, dispatch_state, revoked_offer, at, annotation_id`.

### `ticket_assign`

**Deprecated; hidden by default.** Atomically assign or unassign an open ticket as a coordinator.

Required authorization: `board:coordinate` (deprecated compatibility tool).

| Argument | Type | Presence | Default |
|---|---|---|---|
| `board_id` | `string` | required | — |
| `agent_name` | `string` | required | — |
| `ticket_id` | `string` | required | — |
| `assigned_to_agent_id` | `string | null` | required | — |
| `expected_status` | `string` | required | — |
| `coordinator_op_key` | `string` | required | — |
| `reason` | `string` | required | — |
| `expected_assigned_to_agent_id` | `string | null` | optional | `null` |
| `expected_generation` | `string | null` | optional | `null` |

Response fields: `ok, ticket, event, event_created, idempotent_replay, dispatch_events`.

### `ticket_claim`

Claim only a ticket assigned to the authenticated agent identity.

Required authorization: `board:write` and a live offer or assignment for this seat.

| Argument | Type | Presence | Default |
|---|---|---|---|
| `board_id` | `string` | required | — |
| `agent_name` | `string` | required | — |
| `ticket_id` | `string` | required | — |
| `expected_generation` | `string | null` | optional | `null` |

Compact response fields: `ok, ticket_id, status, parked, generation, dispatch_state, revoked_offer, at`.

### `ticket_unclaim`

Release a held pre-submission ticket back to the open queue.

Required authorization: `board:write` and the current work lease.

| Argument | Type | Presence | Default |
|---|---|---|---|
| `board_id` | `string` | required | — |
| `agent_name` | `string` | required | — |
| `ticket_id` | `string` | required | — |
| `expected_generation` | `string | null` | optional | `null` |

Compact response fields: `ok, ticket_id, status, parked, generation, dispatch_state, revoked_offer, at`.

### `lease_renew`

Renew the authenticated holder's work or review lease.

Required authorization: `board:write` for work or `board:review` for review, with the current lease.

| Argument | Type | Presence | Default |
|---|---|---|---|
| `board_id` | `string` | required | — |
| `ticket_id` | `string` | required | — |
| `expected_generation` | `string | null` | optional | `null` |
| `agent_name` | `string | null` | optional | `null` |
| `renewal_source` | `string | null` | optional | `null` |

Compact response fields: `ok, ticket_id, lease_expires_at, at`.

### `board_reap`

Release expired pre-submission leases; submitted work is durable.

Required authorization: `board:write`.

| Argument | Type | Presence | Default |
|---|---|---|---|
| `board_id` | `string` | required | — |
| `expected_generation` | `string | null` | optional | `null` |

Response fields: `ok, released, release_events, implicitly_renewed, post_submission_preserved`.

### `ticket_submit`

Submit only work claimed by this authenticated agent identity.

Required authorization: `board:write` and the current work lease.

| Argument | Type | Presence | Default |
|---|---|---|---|
| `board_id` | `string` | required | — |
| `agent_name` | `string` | required | — |
| `ticket_id` | `string` | required | — |
| `summary` | `string | null` | optional | `null` |
| `files_changed` | `array[string] | null` | optional | `null` |
| `notes` | `string | null` | optional | `null` |
| `stay_active` | `boolean` | optional | `true` |
| `expected_generation` | `string | null` | optional | `null` |
| `submission_preflight` | `object[string, string] | null` | optional | `null` |
| `model_usage` | `object | null` | optional | `null` |

Response fields: `ok, ticket, event, dispatch_event, release_events, implicitly_renewed, scrub_audit`.

### `ticket_cancel`

Cancel a live ticket as its creator, current executor, or a reviewer.

Required authorization: `board:write` or `board:review`; creator/executor/reviewer checks apply.

| Argument | Type | Presence | Default |
|---|---|---|---|
| `board_id` | `string` | required | — |
| `agent_name` | `string` | required | — |
| `ticket_id` | `string` | required | — |
| `reason` | `string | null` | optional | `null` |
| `expected_generation` | `string | null` | optional | `null` |

Response fields: `ok, ticket, permission, event, release_events, implicitly_renewed, scrub_audit`.

### `dispatch_my_offers`

Return only current, valid dispatch offers for one caller-owned seat. This is a pure recovery read for clients that missed a recipient-scoped push notification. The projection is deliberately minimal and never exposes another seat's offer.

Required authorization: `board:read` for the caller-owned seat.

| Argument | Type | Presence | Default |
|---|---|---|---|
| `board_id` | `string` | required | — |
| `agent_name` | `string` | required | — |

Response fields: `ok, board_id, agent_id, tickets, count, total_matching, truncated, latest_seq`.

### `ticket_list`

List authorized tickets with bounded server-side filters. Terminal closed/canceled tickets that aged into the archive tier stay visible through the compact hot-document index; their full bodies are read through from the archive for the returned page only. ``include_archived=False`` restricts results to the hot document.

Required authorization: `board:read`; server-side visibility filters still apply.

| Argument | Type | Presence | Default |
|---|---|---|---|
| `board_id` | `string` | required | — |
| `status` | `string | null` | optional | `null` |
| `assigned_to` | `string | null` | optional | `null` |
| `include_closed` | `boolean` | optional | `false` |
| `include_archived` | `boolean` | optional | `true` |
| `limit` | `integer` | optional | `100` |
| `agent_name` | `string | null` | optional | `null` |
| `review_unclaimed_only` | `boolean` | optional | `false` |
| `ticket_ids` | `array[string] | null` | optional | `null` |
| `view` | `string` | optional | `"work"` |
| `include_dispatch_history` | `boolean` | optional | `false` |

Response fields: `ok, tickets, count, total_matching, archived_matching, filters, latest_seq`.

## Review

### `ticket_review_claim`

Atomically reserve one submitted ticket for one reviewer seat.

Required authorization: `board:review` and a live review offer or assignment.

| Argument | Type | Presence | Default |
|---|---|---|---|
| `board_id` | `string` | required | — |
| `agent_name` | `string` | required | — |
| `ticket_id` | `string` | required | — |
| `expected_generation` | `string | null` | optional | `null` |

Response fields: `ok, error, release_events, ticket, review_lease, event, event_created, implicitly_renewed`.

### `ticket_review_release`

Release a review reservation held by the current reviewer seat.

Required authorization: `board:review` and the current review lease.

| Argument | Type | Presence | Default |
|---|---|---|---|
| `board_id` | `string` | required | — |
| `agent_name` | `string` | required | — |
| `ticket_id` | `string` | required | — |
| `reason` | `string | null` | optional | `null` |
| `expected_generation` | `string | null` | optional | `null` |

Response fields: `ok, ticket, event, dispatch_event, release_events, implicitly_renewed, scrub_audit`.

### `ticket_review`

Review under the board's strict or workflow policy with board:review.

Required authorization: `board:review` and the current review lease.

| Argument | Type | Presence | Default |
|---|---|---|---|
| `board_id` | `string` | required | — |
| `agent_name` | `string` | required | — |
| `ticket_id` | `string` | required | — |
| `verdict` | `string` | required | — |
| `review_notes` | `string | null` | optional | `null` |
| `fix_instructions` | `string | null` | optional | `null` |
| `expected_generation` | `string | null` | optional | `null` |
| `model_usage` | `object | null` | optional | `null` |

Response fields: `ok, error, release_events, implicitly_renewed, ticket, event, review_claim_event, review_release_event, dispatch_event, retryable_rejection, scrub_audit`.

## Questions & human input

### `ticket_request_human`

Pause work, release its lease, and request structured human input.

Required authorization: `board:write` or `board:coordinate`; ticket authority checks apply.

| Argument | Type | Presence | Default |
|---|---|---|---|
| `board_id` | `string` | required | — |
| `agent_name` | `string` | required | — |
| `ticket_id` | `string` | required | — |
| `message` | `string` | required | — |
| `kind` | `string` | required | — |
| `requested_schema` | `object | null` | optional | `null` |
| `url` | `string | null` | optional | `null` |
| `expected_generation` | `string | null` | optional | `null` |

Response fields: `ok, request_id, ticket, event, release_events, implicitly_renewed, scrub_audit`.

### `ticket_human_resolve`

Resolve a pending human request as a board admin or coordinator.

Required authorization: `board:write` as admin, or `board:coordinate`.

| Argument | Type | Presence | Default |
|---|---|---|---|
| `board_id` | `string` | required | — |
| `agent_name` | `string` | required | — |
| `ticket_id` | `string` | required | — |
| `request_id` | `string` | required | — |
| `action` | `string` | required | — |
| `content` | `any | null` | optional | `null` |
| `note` | `string | null` | optional | `null` |
| `disposition` | `string` | optional | `"reopen"` |
| `expected_generation` | `string | null` | optional | `null` |

Response fields: `ok, ticket, event, dispatch_event, release_events, implicitly_renewed, scrub_audit`.

### `ticket_question_ask`

Ask the project coordinator a durable question without pausing work.

Required authorization: `board:write` with ticket dispatch context.

| Argument | Type | Presence | Default |
|---|---|---|---|
| `board_id` | `string` | required | — |
| `agent_name` | `string` | required | — |
| `ticket_id` | `string` | required | — |
| `message` | `string` | required | — |
| `kind` | `string` | required | — |
| `message_id` | `string | null` | optional | `null` |
| `in_reply_to` | `string | null` | optional | `null` |
| `expected_generation` | `string | null` | optional | `null` |

Response fields: `ok, duplicate, question_id, question, event, release_events, implicitly_renewed, scrub_audit`.

### `board_question_inbox`

List coordinator questions this principal is authorized to answer.

Required authorization: `board:coordinate` and project-coordinator binding.

| Argument | Type | Presence | Default |
|---|---|---|---|
| `board_id` | `string` | required | — |
| `agent_name` | `string` | required | — |
| `state` | `string | null` | optional | `null` |
| `ticket_id` | `string | null` | optional | `null` |
| `limit` | `integer` | optional | `20` |

Response fields: `ok, questions, total, release_events, implicitly_renewed`.

### `ticket_question_answer`

Accept or answer a coordinator question without changing ticket state.

Required authorization: `board:coordinate` and project-coordinator binding.

| Argument | Type | Presence | Default |
|---|---|---|---|
| `board_id` | `string` | required | — |
| `agent_name` | `string` | required | — |
| `ticket_id` | `string` | required | — |
| `question_id` | `string` | required | — |
| `host_binding` | `string` | required | — |
| `action` | `string` | optional | `"answer"` |
| `message` | `string | null` | optional | `null` |
| `expected_generation` | `string | null` | optional | `null` |

Response fields: `ok, duplicate, question, event, release_events, implicitly_renewed, scrub_audit`.

## Memory

### `memory_write`

Write one private/project memory; archived=true preserves oversize content.

Required authorization: `board:write`.

| Argument | Type | Presence | Default |
|---|---|---|---|
| `board_id` | `string` | required | — |
| `agent_name` | `string` | required | — |
| `title` | `string` | required | — |
| `content` | `string` | required | — |
| `scope` | `string` | required | — |
| `memory_type` | `string` | optional | `"context"` |
| `tags` | `array[string] | null` | optional | `null` |
| `priority` | `integer` | optional | `0` |
| `pinned_summary` | `string | null` | optional | `null` |
| `retracts` | `string | null` | optional | `null` |
| `related_files` | `array[string] | null` | optional | `null` |
| `related_tickets` | `array[string] | null` | optional | `null` |
| `archived` | `boolean` | optional | `false` |
| `archive_source_id` | `string | null` | optional | `null` |
| `archived_at` | `string | null` | optional | `null` |
| `expected_generation` | `string | null` | optional | `null` |

Compact response fields: `ok, memory_id, scope, generation, at`.

### `memory_read`

Return visible live memories, plus archives when explicitly requested.

Required authorization: `board:read` and memory visibility.

| Argument | Type | Presence | Default |
|---|---|---|---|
| `board_id` | `string` | required | — |
| `agent_name` | `string` | required | — |
| `memory_type` | `string | null` | optional | `null` |
| `tag` | `string | null` | optional | `null` |
| `author` | `string | null` | optional | `null` |
| `since` | `string | null` | optional | `null` |
| `since_minutes` | `integer | null` | optional | `null` |
| `pinned_only` | `boolean` | optional | `false` |
| `include_archived` | `boolean` | optional | `false` |
| `limit` | `integer` | optional | `50` |
| `expected_generation` | `string | null` | optional | `null` |

Response fields: `ok, memories, visible_count, total_matching, release_events, implicitly_renewed`.

### `memory_unpin`

Demote a visible authorized memory while retaining its audit record.

Required authorization: `board:write` plus author/admin/coordinator authority.

| Argument | Type | Presence | Default |
|---|---|---|---|
| `board_id` | `string` | required | — |
| `agent_name` | `string` | required | — |
| `memory_id` | `string` | required | — |
| `reason` | `string | null` | optional | `null` |
| `expected_generation` | `string | null` | optional | `null` |

Response fields: `ok, changed, memory, event, release_events, implicitly_renewed`.

### `memory_search`

Search visible live memories, plus archives when explicitly requested.

Required authorization: `board:read` and memory visibility.

| Argument | Type | Presence | Default |
|---|---|---|---|
| `board_id` | `string` | required | — |
| `query` | `string` | required | — |
| `agent_name` | `string | null` | optional | `null` |
| `tag` | `string | null` | optional | `null` |
| `author` | `string | null` | optional | `null` |
| `include_archived` | `boolean` | optional | `false` |
| `limit` | `integer` | optional | `20` |

Response fields: `ok, query, results, count, total_matching, include_archived`.

### `memory_links`

Traverse visible retraction links and return explicit ticket/file/tag edges.

Required authorization: `board:read` and memory visibility.

| Argument | Type | Presence | Default |
|---|---|---|---|
| `board_id` | `string` | required | — |
| `agent_name` | `string | null` | optional | `null` |
| `memory_id` | `string | null` | optional | `null` |
| `ticket_id` | `string | null` | optional | `null` |
| `file` | `string | null` | optional | `null` |
| `author` | `string | null` | optional | `null` |
| `depth` | `integer` | optional | `2` |
| `limit` | `integer` | optional | `50` |

Response fields: `ok, nodes, edges, node_count, edge_count, depth`.

### `memory_checkpoint`

Persist a structured checkpoint memory atomically with agent activity.

Required authorization: `board:write`.

| Argument | Type | Presence | Default |
|---|---|---|---|
| `board_id` | `string` | required | — |
| `agent_name` | `string` | required | — |
| `summary` | `string` | required | — |
| `remaining_tasks` | `array[string] | null` | optional | `null` |
| `files` | `array[string] | null` | optional | `null` |
| `next_steps` | `array[string] | null` | optional | `null` |
| `active_branch` | `string | null` | optional | `null` |
| `blockers` | `array[string] | null` | optional | `null` |
| `scope` | `string` | optional | `"project"` |
| `expected_generation` | `string | null` | optional | `null` |

Compact response fields: `ok, memory_id, scope, generation, at`.

### `memory_handoff`

Write a project handoff and flip the exact identity to handed_off.

Required authorization: `board:write` for the exact seat identity.

| Argument | Type | Presence | Default |
|---|---|---|---|
| `board_id` | `string` | required | — |
| `agent_name` | `string` | required | — |
| `summary` | `string` | required | — |
| `next_steps` | `array[string]` | required | — |
| `files` | `array[string] | null` | optional | `null` |
| `warnings` | `array[string] | null` | optional | `null` |
| `expected_generation` | `string | null` | optional | `null` |

Response fields: `ok, memory, lifecycle_status, superseded_handoffs, event, release_events`.

## State

### `board_status`

Return a pure structured and rendered board status summary.

Required authorization: `board:read`.

| Argument | Type | Presence | Default |
|---|---|---|---|
| `board_id` | `string` | required | — |
| `include_retired` | `boolean` | optional | `false` |

Response fields: `ok, board_id, agents, claim_ttl_s, stale_after_days, journal_retention_days, journal_row_cap, retired_or_stale_count, scrub_profile, review_policy, response_view, dispatch_enabled, dispatch_policy, unassignable_tickets, unclaimed_tickets, review_label_counts, scrub_allow_counts, ticket_status_counts, memory_type_counts, visible_memory_count, latest_seq, rendered`.

### `board_state_update`

Atomically set one project-scoped board state value.

Required authorization: `board:write`, or restricted `board:coordinate`/`board:intake`.

| Argument | Type | Presence | Default |
|---|---|---|---|
| `board_id` | `string` | required | — |
| `agent_name` | `string` | required | — |
| `key` | `string` | required | — |
| `value` | `string` | required | — |
| `expected_generation` | `string | null` | optional | `null` |
| `expected_sha256` | `string | null` | optional | `null` |

Response fields: `ok, key, state, release_events, implicitly_renewed`.

### `board_state_get`

Read one or all project-scoped board state values without mutation.

Required authorization: `board:read`, or restricted `board:intake`.

| Argument | Type | Presence | Default |
|---|---|---|---|
| `board_id` | `string` | required | — |
| `key` | `string | null` | optional | `null` |

Response fields: `ok, scope, key, state`.

## Events & wait

### `board_dispatch_events`

Return a bounded cross-seat projection of recent dispatch events. This member-authorized view intentionally omits event actors, recipient identities, agent IDs, and payload references. ``board_catchup`` keeps its recipient-scoped visibility contract.

Required authorization: `board:read`.

| Argument | Type | Presence | Default |
|---|---|---|---|
| `board_id` | `string` | required | — |
| `limit` | `integer` | optional | `25` |

Response fields: `ok, board_id, events, latest_seq, scan_count, scan_truncated`.

### `board_catchup`

Drain a byte- and event-bounded journal page. Optionally acknowledge only the cursor represented in this response. ``touch=False`` is a pure wake-cue refetch: it does not update member activity, reap or renew leases, acknowledge a cursor, or validate a write generation. Visibility and response bounds remain unchanged.

Required authorization: `board:read`; `board:write` enables touch/lease renewal side effects.

| Argument | Type | Presence | Default |
|---|---|---|---|
| `board_id` | `string` | required | — |
| `agent_name` | `string` | required | — |
| `cursor` | `integer | null` | optional | `null` |
| `limit` | `integer` | optional | `100` |
| `ack` | `boolean` | optional | `true` |
| `touch` | `boolean` | optional | `true` |
| `max_events` | `integer` | optional | `200` |
| `max_bytes` | `integer` | optional | `300000` |
| `expected_generation` | `string | null` | optional | `null` |

Response fields: `ok, board_id, bounds, events, next_cursor, latest_cursor, has_more, resync_required, compacted_through, reset_cursor, new_seq, scan_count, visible_count, acknowledged_cursor, touched, release_events, total_counts, returned_counts, omitted_counts, truncated`.

## Board admin & policy

### `board_scrub_profile_set`

Set the server-enforced board scrub profile as a board admin.

Required authorization: `board:write` and board-admin membership.

| Argument | Type | Presence | Default |
|---|---|---|---|
| `board_id` | `string` | required | — |
| `agent_name` | `string` | required | — |
| `scrub_profile` | `string` | required | — |
| `expected_generation` | `string | null` | optional | `null` |

Response fields: `ok, board_id, scrub_profile, previous_scrub_profile, changed, scrub_allow_counts, event`.

### `board_response_view_set`

Set compact or full model-facing write responses as a board admin.

Required authorization: `board:write` and board-admin membership.

| Argument | Type | Presence | Default |
|---|---|---|---|
| `board_id` | `string` | required | — |
| `agent_name` | `string` | required | — |
| `response_view` | `string` | required | — |
| `expected_generation` | `string | null` | optional | `null` |

Response fields: `ok, board_id, response_view, previous_response_view, changed, at`.

### `board_review_policy_set`

Set strict authorization or workflow agent cross-check review as admin.

Required authorization: `board:write` and board-admin membership.

| Argument | Type | Presence | Default |
|---|---|---|---|
| `board_id` | `string` | required | — |
| `agent_name` | `string` | required | — |
| `review_policy` | `string` | required | — |
| `expected_generation` | `string | null` | optional | `null` |

Response fields: `ok, board_id, review_policy, previous_review_policy, changed, event`.

### `board_stale_after_set`

Set the inactivity threshold used to auto-stale agent identities.

Required authorization: `board:coordinate`.

| Argument | Type | Presence | Default |
|---|---|---|---|
| `board_id` | `string` | required | — |
| `agent_name` | `string` | required | — |
| `stale_after_days` | `integer` | required | — |
| `expected_generation` | `string | null` | optional | `null` |

Response fields: `ok, board_id, stale_after_days, previous_stale_after_days, changed, renewed_ticket_ids, events`.

### `board_journal_retention_set`

Set the journal time window and hard row cap for one board.

Required authorization: `board:coordinate`.

| Argument | Type | Presence | Default |
|---|---|---|---|
| `board_id` | `string` | required | — |
| `agent_name` | `string` | required | — |
| `journal_retention_days` | `integer` | required | — |
| `journal_row_cap` | `integer` | required | — |
| `expected_generation` | `string | null` | optional | `null` |

Response fields: `ok, board_id, journal_retention_days, previous_journal_retention_days, journal_row_cap, previous_journal_row_cap, changed, journal_compaction, renewed_ticket_ids, events`.

### `board_dispatch_policy_set`

Set per-seat offer expiry and fallback behavior as board admin.

Required authorization: `board:write` as admin, or `board:coordinate`.

| Argument | Type | Presence | Default |
|---|---|---|---|
| `board_id` | `string` | required | — |
| `agent_name` | `string` | required | — |
| `offer_ttl_s` | `integer` | optional | `120` |
| `broadcast_reoffer_s` | `integer` | optional | `600` |
| `second_opinion` | `boolean` | optional | `true` |
| `fallback_broadcast` | `boolean` | optional | `true` |
| `expected_generation` | `string | null` | optional | `null` |

Response fields: `ok, board_id, dispatch_policy, previous_dispatch_policy, changed`.

### `board_claim_ttl_set`

Set the live per-board claim TTL as an admin or coordinator seat.

Required authorization: `board:write` as admin, or `board:coordinate`.

| Argument | Type | Presence | Default |
|---|---|---|---|
| `board_id` | `string` | required | — |
| `agent_name` | `string` | required | — |
| `claim_ttl_s` | `integer` | required | — |
| `expected_generation` | `string | null` | optional | `null` |

Response fields: `ok, board_id, claim_ttl_s, previous_claim_ttl_s, changed, event`.

## Retention & archive

### `journal_compact`

Manually compact only derivable journal telemetry. Durable tickets, memories, agents, and consumer cursors are never deleted or modified.

Required authorization: `board:write` and board-admin membership.

| Argument | Type | Presence | Default |
|---|---|---|---|
| `board_id` | `string` | required | — |
| `retain_last` | `integer` | required | — |

Response fields: `ok, board_id, compacted_through, removed, retained, latest_cursor, durable_records_untouched`.

### `board_archive_run`

Manual operator sweep; the automatic reaper-timer sweep is primary. Runs the same automatic bloat rules on demand: archives closed/ canceled tickets older than ``archive_after_days`` (default 2) into per-ticket archive documents, bounds inline histories to ``inline_history_limit`` (default 50) newest entries, tombstones retired members inactive for ``stale_after_days``, prunes invites expired more than ``invite_prune_after_days`` (default 7), journals one ``ticket_archived`` entry with counts for board admins, and compacts journal rows beyond the retention window (oldest live consumer cursor plus ``journal_retention_days``, default 7, with a 500-row floor). Durable archived records are never deleted; archived tickets stay readable through ticket_get and ticket_list.

Required authorization: `board:write` and admin membership, or `board:coordinate`.

| Argument | Type | Presence | Default |
|---|---|---|---|
| `board_id` | `string` | required | — |

Response fields: `ok, board_id, archived_ticket_count, bounded_history_count, tombstoned_member_count, pruned_invite_count, journal_compaction, durable_records_untouched`.

## Registry

### `board_list`

List only boards containing the authenticated principal.

Required authorization: `board:read`.

| Argument | Type | Presence | Default |
|---|---|---|---|
| — | — | — | — |

Response fields: `ok, boards`.
