# Command-line reference

Generated by `python3 tools/generate_reference_docs.py`. The blocks below are literal `--help` output captured from this source tree.

The Fleet dashboard and Board Butler are repository launchers rather than installed console scripts. Their sections use `python3 tools/fleet-dashboard/fleet_dashboard.py` and `python3 tools/board-butler/board_butler.py` respectively.

## pursers-central

### `pursers-central --help`

```text
usage: pursers-central [-h] [--host HOST] [--port PORT] [--data-dir DATA_DIR]
                       [--log-level {critical,error,warning,info,debug,trace}] [--tls-certfile TLS_CERTFILE]
                       [--tls-keyfile TLS_KEYFILE] [--allowed-host HOST] [--advance-generation BOARD_ID]
                       [--expect-generation-sha256 HEX]

Run On Board Central

options:
  -h, --help            show this help message and exit
  --host HOST
  --port PORT
  --data-dir, --data-root DATA_DIR
  --log-level {critical,error,warning,info,debug,trace}
  --tls-certfile, --ssl-certfile TLS_CERTFILE
                        TLS certificate supplied by the operator (requires --tls-keyfile)
  --tls-keyfile, --ssl-keyfile TLS_KEYFILE
                        TLS private key supplied by the operator (requires --tls-certfile)
  --allowed-host HOST   additional bare Host name; repeat the flag or set the comma-separated
                        ONBOARD_CENTRAL_ALLOWED_HOSTS value
  --advance-generation BOARD_ID
  --expect-generation-sha256 HEX

For a local first run: pursers-central init DIR, then pursers-central run DIR.
```

### `pursers-central init --help`

```text
usage: pursers-central init [-h] [--port PORT] [--board BOARD] [--force] directory

Create a private local Central profile and credentials

positional arguments:
  directory

options:
  -h, --help     show this help message and exit
  --port PORT
  --board BOARD
  --force        hard cutover: replace credentials immediately; use rotate-key for overlap
```

### `pursers-central run --help`

```text
usage: pursers-central run [-h] directory

Run Central from a generated quickstart profile

positional arguments:
  directory

options:
  -h, --help  show this help message and exit
```

### `pursers-central rotate-key --help`

```text
usage: pursers-central rotate-key [-h] [--key KEY] [--jwks JWKS] [--token TOKEN] [directory]

Rotate an issuer key while keeping the old key valid

positional arguments:
  directory

options:
  -h, --help     show this help message and exit
  --key KEY
  --jwks JWKS
  --token TOKEN
```

### `pursers-central retire-key --help`

```text
usage: pursers-central retire-key [-h] [--key KEY] [--jwks JWKS] --kid KID [--check-token CHECK_TOKEN] [directory]

Retire an inactive issuer key after clients reconnect

positional arguments:
  directory

options:
  -h, --help            show this help message and exit
  --key KEY
  --jwks JWKS
  --kid KID
  --check-token CHECK_TOKEN
```

## pursers-personal

### `pursers-personal --help`

```text
usage: pursers-personal [-h] [--version] [--json]
                        {setup,profiles,doctor,central,mcp,rotate,restart,rollback,uninstall} ...

Single user-facing console for Pursers Personal.

positional arguments:
  {setup,profiles,doctor,central,mcp,rotate,restart,rollback,uninstall}
    setup               create a profile and plan/apply local integration
    profiles            list profiles and safely prune verified orphans
    doctor              show effective identity and actionable checks
    central             run the strict loopback Central service
    mcp                 run the profile-backed Apps/chat stdio facade
    rotate              rotate the local signing key and capability
    restart             restart only the owned Personal service
    rollback            restore exact pre-setup integration bytes
    uninstall           remove integration but retain profile/data

options:
  -h, --help            show this help message and exit
  --version             show program's version number and exit
  --json                emit JSON only
```

### `pursers-personal setup --help`

```text
usage: pursers-personal setup [-h] --project PROJECT [--profiles-root PROFILES_ROOT] [--port PORT] [--host-id HOST_ID]
                              [--session SESSION] [--console CONSOLE] [--launch-agents-dir LAUNCH_AGENTS_DIR]
                              [--host-config HOST_CONFIG] [--apply] [--activate]

options:
  -h, --help            show this help message and exit
  --project PROJECT
  --profiles-root PROFILES_ROOT
  --port PORT           explicit loopback port; omitted chooses and persists an available port
  --host-id HOST_ID
  --session SESSION
  --console CONSOLE
  --launch-agents-dir LAUNCH_AGENTS_DIR
  --host-config HOST_CONFIG
  --apply
  --activate
```

### `pursers-personal profiles --help`

```text
usage: pursers-personal profiles [-h] {list,prune} ...

positional arguments:
  {list,prune}
    list        list profile references
    prune       remove only verified orphan profiles

options:
  -h, --help    show this help message and exit
```

### `pursers-personal profiles list --help`

```text
usage: pursers-personal profiles list [-h] [--profiles-root PROFILES_ROOT] [--host-config HOST_CONFIG]
                                      [--launch-agents-dir LAUNCH_AGENTS_DIR]

options:
  -h, --help            show this help message and exit
  --profiles-root PROFILES_ROOT
  --host-config HOST_CONFIG
                        additional host config to inspect for profile references
  --launch-agents-dir LAUNCH_AGENTS_DIR
                        additional LaunchAgents directory to inspect
```

### `pursers-personal profiles prune --help`

```text
usage: pursers-personal profiles prune [-h] [--profiles-root PROFILES_ROOT] [--host-config HOST_CONFIG]
                                       [--launch-agents-dir LAUNCH_AGENTS_DIR] --orphaned (--dry-run | --commit)

options:
  -h, --help            show this help message and exit
  --profiles-root PROFILES_ROOT
  --host-config HOST_CONFIG
                        additional host config to inspect for profile references
  --launch-agents-dir LAUNCH_AGENTS_DIR
                        additional LaunchAgents directory to inspect
  --orphaned
  --dry-run
  --commit
```

### `pursers-personal doctor --help`

```text
usage: pursers-personal doctor [-h] [--profile PROFILE] [--project PROJECT] [--profiles-root PROFILES_ROOT]
                               [--host-id HOST_ID] [--session SESSION]

options:
  -h, --help            show this help message and exit
  --profile PROFILE
  --project PROJECT
  --profiles-root PROFILES_ROOT
  --host-id HOST_ID
  --session SESSION
```

### `pursers-personal central --help`

```text
usage: pursers-personal central [-h] [--profile PROFILE] [--project PROJECT] [--profiles-root PROFILES_ROOT]

options:
  -h, --help            show this help message and exit
  --profile PROFILE
  --project PROJECT
  --profiles-root PROFILES_ROOT
```

### `pursers-personal mcp --help`

```text
usage: pursers-personal mcp [-h] --profile PROFILE --host-id HOST_ID --session SESSION
                            [--acceptance-runtime-receipt ACCEPTANCE_RUNTIME_RECEIPT]
                            [--acceptance-challenge-key ACCEPTANCE_CHALLENGE_KEY]
                            [--candidate-source CANDIDATE_SOURCE] [--candidate-commit CANDIDATE_COMMIT]
                            [--board-id BOARD_ID]

options:
  -h, --help            show this help message and exit
  --profile PROFILE
  --host-id HOST_ID
  --session SESSION
  --acceptance-runtime-receipt ACCEPTANCE_RUNTIME_RECEIPT
  --acceptance-challenge-key ACCEPTANCE_CHALLENGE_KEY
  --candidate-source CANDIDATE_SOURCE
  --candidate-commit CANDIDATE_COMMIT
  --board-id BOARD_ID
```

### `pursers-personal rotate --help`

```text
usage: pursers-personal rotate [-h] --profile PROFILE [--activate]

options:
  -h, --help         show this help message and exit
  --profile PROFILE
  --activate         restart the owned service
```

### `pursers-personal restart --help`

```text
usage: pursers-personal restart [-h] [--profile PROFILE] [--project PROJECT] [--profiles-root PROFILES_ROOT]
                                [--activate]

options:
  -h, --help            show this help message and exit
  --profile PROFILE
  --project PROJECT
  --profiles-root PROFILES_ROOT
  --activate
```

### `pursers-personal rollback --help`

```text
usage: pursers-personal rollback [-h] [--profile PROFILE] [--project PROJECT] [--profiles-root PROFILES_ROOT]
                                 [--activate]

options:
  -h, --help            show this help message and exit
  --profile PROFILE
  --project PROJECT
  --profiles-root PROFILES_ROOT
  --activate            deprecated compatibility flag; the owned service is always stopped safely
```

### `pursers-personal uninstall --help`

```text
usage: pursers-personal uninstall [-h] [--profile PROFILE] [--project PROJECT] [--profiles-root PROFILES_ROOT]
                                  [--activate]

options:
  -h, --help            show this help message and exit
  --profile PROFILE
  --project PROJECT
  --profiles-root PROFILES_ROOT
  --activate            deprecated compatibility flag; the owned service is always stopped safely
```

## pursers-personal-import

### `pursers-personal-import --help`

```text
usage: pursers-personal-import [-h] {import,retry,rollback,review,decide,status,archive-backfill} ...

Import a stable local board copy into an offline Personal Central root.

positional arguments:
  {import,retry,rollback,review,decide,status,archive-backfill}

options:
  -h, --help            show this help message and exit
```

### `pursers-personal-import import --help`

```text
usage: pursers-personal-import import [-h] --run-dir RUN_DIR --board-id BOARD_ID
                                      --owner-principal-id OWNER_PRINCIPAL_ID --owner-agent-name OWNER_AGENT_NAME
                                      --stable-install-root STABLE_INSTALL_ROOT [--confirm-central-stopped]
                                      source central_data_root

positional arguments:
  source
  central_data_root

options:
  -h, --help            show this help message and exit
  --run-dir RUN_DIR
  --board-id BOARD_ID
  --owner-principal-id OWNER_PRINCIPAL_ID
  --owner-agent-name OWNER_AGENT_NAME
  --stable-install-root STABLE_INSTALL_ROOT
  --confirm-central-stopped
```

### `pursers-personal-import retry --help`

```text
usage: pursers-personal-import retry [-h] [--confirm-central-stopped] run_dir

positional arguments:
  run_dir

options:
  -h, --help            show this help message and exit
  --confirm-central-stopped
```

### `pursers-personal-import rollback --help`

```text
usage: pursers-personal-import rollback [-h] [--confirm-central-stopped] run_dir

positional arguments:
  run_dir

options:
  -h, --help            show this help message and exit
  --confirm-central-stopped
```

### `pursers-personal-import review --help`

```text
usage: pursers-personal-import review [-h] [--bindings BINDINGS] [--decisions DECISIONS] [--confirm-central-stopped]
                                      run_dir

positional arguments:
  run_dir

options:
  -h, --help            show this help message and exit
  --bindings BINDINGS
  --decisions DECISIONS
  --confirm-central-stopped
```

### `pursers-personal-import decide --help`

```text
usage: pursers-personal-import decide [-h] --policy POLICY [--output OUTPUT] run_dir

positional arguments:
  run_dir

options:
  -h, --help       show this help message and exit
  --policy POLICY
  --output OUTPUT
```

### `pursers-personal-import status --help`

```text
usage: pursers-personal-import status [-h] [--confirm-central-stopped] run_dir

positional arguments:
  run_dir

options:
  -h, --help            show this help message and exit
  --confirm-central-stopped
```

### `pursers-personal-import archive-backfill --help`

```text
usage: pursers-personal-import archive-backfill [-h] --board-id BOARD_ID [--scrub-profile {strict,internal}]
                                                [--redact-secrets-record REDACT_SECRETS_RECORD]
                                                [--confirm-central-stopped]
                                                archive_file central_data_root

positional arguments:
  archive_file
  central_data_root

options:
  -h, --help            show this help message and exit
  --board-id BOARD_ID
  --scrub-profile {strict,internal}
  --redact-secrets-record REDACT_SECRETS_RECORD
  --confirm-central-stopped
```

## pursers-wait-bridge

### `pursers-wait-bridge --help`

```text
usage: pursers-wait-bridge [-h] {join,status,forget,ticket-lifecycle,seat-lifecycle,team-lifecycle} ...

Run the Pursers MCP wait bridge or manage its local seat.

positional arguments:
  {join,status,forget,ticket-lifecycle,seat-lifecycle,team-lifecycle}
    join                store a door and onboard a seat
    status              show redacted stored-door status
    forget              delete one stored door
    ticket-lifecycle    run the ticket lifecycle sidecar
    seat-lifecycle      run the seat lifecycle sidecar
    team-lifecycle      run the team lifecycle sidecar

options:
  -h, --help            show this help message and exit
```

### `pursers-wait-bridge join --help`

```text
usage: pursers-wait-bridge join [-h] [--name NAME] [--state-dir STATE_DIR] [--allow-remote] [--rotate] door

positional arguments:
  door

options:
  -h, --help            show this help message and exit
  --name NAME
  --state-dir STATE_DIR
  --allow-remote
  --rotate
```

### `pursers-wait-bridge status --help`

```text
usage: pursers-wait-bridge status [-h] [--state-dir STATE_DIR]

options:
  -h, --help            show this help message and exit
  --state-dir STATE_DIR
```

### `pursers-wait-bridge forget --help`

```text
usage: pursers-wait-bridge forget [-h] --board BOARD --role {reviewer,worker} [--state-dir STATE_DIR]

options:
  -h, --help            show this help message and exit
  --board BOARD
  --role {reviewer,worker}
  --state-dir STATE_DIR
```

### `pursers-wait-bridge ticket-lifecycle --help`

```text
usage: pursers-wait-bridge ticket-lifecycle [-h] --state-dir STATE_DIR --board BOARD

options:
  -h, --help            show this help message and exit
  --state-dir STATE_DIR
  --board BOARD
```

### `pursers-wait-bridge seat-lifecycle --help`

```text
usage: pursers-wait-bridge seat-lifecycle [-h] --state-dir STATE_DIR --board BOARD

options:
  -h, --help            show this help message and exit
  --state-dir STATE_DIR
  --board BOARD
```

### `pursers-wait-bridge team-lifecycle --help`

```text
usage: pursers-wait-bridge team-lifecycle [-h] --state-dir STATE_DIR --board BOARD

options:
  -h, --help            show this help message and exit
  --state-dir STATE_DIR
  --board BOARD
```

## pursers-door

### `pursers-door --help`

```text
usage: pursers-door [-h] {issue,rotate,list,revoke-kid,decode} ...

Manage Pursers door credentials.

positional arguments:
  {issue,rotate,list,revoke-kid,decode}
    issue               issue or refresh a door
    rotate              rotate and revoke a door
    list                list public door metadata
    revoke-kid          remove one public signing key
    decode              show non-secret door metadata

options:
  -h, --help            show this help message and exit
```

### `pursers-door issue --help`

```text
usage: pursers-door issue [-h] --board BOARD (--role {reviewer,worker} | --named) [--sub SUB] [--scope SCOPE]
                          --central-url CENTRAL_URL [--issuer ISSUER] --jwks JWKS --keys-dir KEYS_DIR
                          [--exp-days EXP_DAYS]

options:
  -h, --help            show this help message and exit
  --board BOARD
  --role {reviewer,worker}
  --named
  --sub SUB
  --scope SCOPE
  --central-url CENTRAL_URL
  --issuer ISSUER
  --jwks JWKS
  --keys-dir KEYS_DIR
  --exp-days EXP_DAYS
```

### `pursers-door rotate --help`

```text
usage: pursers-door rotate [-h] --board BOARD (--role {reviewer,worker} | --named) [--sub SUB] [--scope SCOPE]
                           --central-url CENTRAL_URL [--issuer ISSUER] --jwks JWKS --keys-dir KEYS_DIR
                           [--exp-days EXP_DAYS]

options:
  -h, --help            show this help message and exit
  --board BOARD
  --role {reviewer,worker}
  --named
  --sub SUB
  --scope SCOPE
  --central-url CENTRAL_URL
  --issuer ISSUER
  --jwks JWKS
  --keys-dir KEYS_DIR
  --exp-days EXP_DAYS
```

### `pursers-door list --help`

```text
usage: pursers-door list [-h] --jwks JWKS

options:
  -h, --help   show this help message and exit
  --jwks JWKS
```

### `pursers-door revoke-kid --help`

```text
usage: pursers-door revoke-kid [-h] --jwks JWKS kid

positional arguments:
  kid

options:
  -h, --help   show this help message and exit
  --jwks JWKS
```

### `pursers-door decode --help`

```text
usage: pursers-door decode [-h] door_string

positional arguments:
  door_string

options:
  -h, --help   show this help message and exit
```

## pursers-acp

### `pursers-acp --help`

```text
usage: pursers-acp [-h] [--profile PROFILE] [--project PROJECT] [--login]

options:
  -h, --help         show this help message and exit
  --profile PROFILE
  --project PROJECT
  --login
```

## Fleet dashboard launcher

### `python3 tools/fleet-dashboard/fleet_dashboard.py --help`

```text
usage: fleet_dashboard.py [-h] [--port PORT] [--url URL] [--token-file TOKEN_FILE] [--doors-keys-dir DOORS_KEYS_DIR]
                          [--jwks-path JWKS_PATH] [--centrals CENTRALS] [--home-board HOME_BOARD]
                          [--agent-name AGENT_NAME] [--stale-seconds STALE_SECONDS] [--cache-seconds CACHE_SECONDS]
                          [--workers-dir WORKERS_DIR] [--butler-secrets-dir BUTLER_SECRETS_DIR]
                          [--evidence-trace-config EVIDENCE_TRACE_CONFIG]

Run the loopback fleet dashboard

options:
  -h, --help            show this help message and exit
  --port PORT
  --url URL
  --token-file TOKEN_FILE
  --doors-keys-dir DOORS_KEYS_DIR
                        Directory for door RSA private keys (default: $PURSERS_DOORS_KEYS_DIR)
  --jwks-path JWKS_PATH
                        Path to public JWKS file (default: $PURSERS_JWKS_PATH)
  --centrals CENTRALS   0600 JSON list of {label,url,token_path,home_board[,stats_path]} entries
  --home-board HOME_BOARD
  --agent-name AGENT_NAME
                        Reserved dashboard session identity (fleet-dashboard-session-*)
  --stale-seconds STALE_SECONDS
  --cache-seconds CACHE_SECONDS
  --workers-dir WORKERS_DIR
  --butler-secrets-dir BUTLER_SECRETS_DIR
                        Private 0700 directory for write-only Board Butler keys
  --evidence-trace-config EVIDENCE_TRACE_CONFIG
                        Verifier-owned 0600 config for bounded Fleet evidence tracing
```

## Board Butler

### `python3 tools/board-butler/board_butler.py --help`

```text
usage: board_butler.py [-h] [--url URL] --token-path TOKEN_PATH [--home-board HOME_BOARD] [--agent-name AGENT_NAME]
                       --repo REPO [--integration-ref INTEGRATION_REF] --pid-file PID_FILE --cursor-file CURSOR_FILE
                       [--runtime-status-file RUNTIME_STATUS_FILE] [--local-kill-file LOCAL_KILL_FILE]
                       [--runtime-mode {shadow,active}] [--active-authorization-file ACTIVE_AUTHORIZATION_FILE]
                       [--provider-secrets-dir PROVIDER_SECRETS_DIR] [--drafts-per-hour DRAFTS_PER_HOUR]
                       [--drafts-per-ticket DRAFTS_PER_TICKET] [--drafts-per-board DRAFTS_PER_BOARD]
                       [--project PROJECT] [--wait-timeout WAIT_TIMEOUT] [--refresh-seconds REFRESH_SECONDS]
                       [--act-on-board BOARD_ID] [--no-live-candidates-cycles NO_LIVE_CANDIDATES_CYCLES]
                       [--active-action {park_no_live_candidates,refuse_incapable_target}]
                       [--action-hold-seconds ACTION_HOLD_SECONDS] [--once] [--dry-run] [--kill-switch |
                       --veto-question VETO_QUESTION] [--control-reason CONTROL_REASON]

Registry-wide coordinator findings refresher and shadow question drafter. The butler runs the real coordinator
derivation for every active registry board on a bounded cycle and listens for coordinator questions through the same
journal/seat resource subscriptions used by the wait bridge. It never answers a question. Its Central writes are CAS-
protected findings and identifier-only evaluation records; the only ticket mutations are two explicitly configured,
mechanically checkable safety actions: parking repeated ``no_live_candidates`` loops and recording refusal of an
escalation target that cannot work.

options:
  -h, --help            show this help message and exit
  --url URL
  --token-path TOKEN_PATH
  --home-board HOME_BOARD
  --agent-name AGENT_NAME
  --repo REPO
  --integration-ref INTEGRATION_REF
  --pid-file PID_FILE
  --cursor-file CURSOR_FILE
  --runtime-status-file RUNTIME_STATUS_FILE
  --local-kill-file LOCAL_KILL_FILE
  --runtime-mode {shadow,active}
                        local service mode; active additionally requires an authorization file
  --active-authorization-file ACTIVE_AUTHORIZATION_FILE
  --provider-secrets-dir PROVIDER_SECRETS_DIR
  --drafts-per-hour DRAFTS_PER_HOUR
  --drafts-per-ticket DRAFTS_PER_TICKET
  --drafts-per-board DRAFTS_PER_BOARD
  --project PROJECT
  --wait-timeout WAIT_TIMEOUT
  --refresh-seconds REFRESH_SECONDS
                        maximum interval between real coordinator derivations
  --act-on-board BOARD_ID
                        explicitly opt one active registry board into the two mechanical ticket actions; repeat for
                        multiple boards
  --no-live-candidates-cycles NO_LIVE_CANDIDATES_CYCLES
  --active-action {park_no_live_candidates,refuse_incapable_target}
                        enabled autonomous action class; repeat to configure the acting set (defaults to the two
                        operator-approved mechanical classes)
  --action-hold-seconds ACTION_HOLD_SECONDS
                        veto window before an enabled mechanical action can execute
  --once
  --dry-run
  --kill-switch
  --veto-question VETO_QUESTION
  --control-reason CONTROL_REASON
```
