# Minimal Zed extension fixture

This fixture exists only to exercise the standalone-export and registry-validation
tooling before the real `integrations/zed/pursers-mcp` source lands. Runtime or Zed
acceptance must not be inferred from it. Evidence must label it as a fixture.
